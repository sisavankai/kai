// ຂໍ້ມູນ user ທີ່ຈະສ້າງເປັນຕາຕະລາງ
// id, firstname, lastname, email, password, avatar, timestamp
interface user {
    id: Number
    firstname: string
    lastname: string
    email: string
    password: string
    avatar: string
    timestamp: number
}

export default user